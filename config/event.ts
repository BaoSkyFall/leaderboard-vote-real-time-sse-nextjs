// Event-wide configuration.

export const EVENT_TITLE = "GEM RAP BATTLE";

/** Default vote duration in seconds (4 minutes). */
export const DEFAULT_DURATION_SEC = 240;

/** Allowed bounds for the BTC-configurable duration (seconds). */
export const MIN_DURATION_SEC = 30;
export const MAX_DURATION_SEC = 60 * 30; // 30 minutes
